#' Groups for Cognitive Variables
#'
#' @format ## `cog_vars_groups`
#' \describe{Named vector given groups (in the entries) for cognitive variables (in names).
#' For example, `cog_vars_groups["cog_cdr_global"]` returns the group `cog_cdr_global`
#' belongs to.
#' }
#'
"cog_vars_groups"
